<%-include("partials/header")-%>
    <h1>home</h1>
    <p>
        <%=homeContent%>
    </p>
    <% posts.forEach((element) => { %>
        <h1>
            <%=element.title%>
        </h1>
        <p>
            <%=element.content.slice(0,100) + "..."%> <a href="/posts/<%= element._id %> ">Read more</a>
        </p>
        <% }) %>
            <%-include("partials/footer")-%>